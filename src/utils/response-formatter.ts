// src/utils/response-formatter.ts
// Note: This file is currently disabled due to H3 dependencies
// The v1 routes use Hono and handle responses directly

export interface ResponseOptions {
  statusCode?: number;
  headers?: Record<string, string>;
}

export class ResponseFormatter {
  // Placeholder class - methods commented out due to H3 dependencies
  // TODO: Refactor to work with Hono if needed
}